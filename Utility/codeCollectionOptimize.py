# Problem with Version
import pymongo						
from pymongo import MongoClient

client=MongoClient()
db=client['IBM']
codeCollection=db['Code']
elementUsageDefs=db['ElementUsageDefs']

_id=collection.insert_one(res)
print("Status : "+str(i)+"/"+str(len(content)-1))

fo.close();